# SUPERSEDED — the root .env.example at the project root replaces this file.
# Copy /.env.example to /.env and edit that instead.
# This file can be safely deleted.

PORT=3000
PIN=changeme
# Session tokens expire after this many milliseconds (default 24h)
SESSION_TTL_MS=86400000
